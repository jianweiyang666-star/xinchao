# 产品需求文档 (PRD) - 心潮(InnerTide)

## 1. 产品概述 (Product Overview)
**项目名称**：心潮 (InnerTide)
**产品愿景**：一款专为女性打造的、富有诗意和情感温度的经期追踪与自我关怀（Journaling）应用。它摒弃了传统经期App冰冷的医疗感，以“潮汐”、“岛屿”与“四季”作为生命周期的隐喻，为女性提供经前综合征（PMS）、经期及整个生理周期中的疗愈陪伴。  
**目标用户**：关注自我探索、情绪健康以及生理健康，并需要高审美、温暖陪伴体验的女性用户。

## 2. 核心体验与设计语言 (Core Experience & Design Language)
- **视觉风格**：极简、柔和、疗愈。色彩多使用低饱和度的治愈系颜色（如大地色、微光粉、米白）。
- **生命隐喻**：
  - 月经期 (Menstrual) ➡️ 冬：蛰伏与释放
  - 卵泡期 (Follicular) ➡️ 春：苏醒与升起
  - 排卵期 (Ovulatory) ➡️ 夏：盛放与巅峰
  - 黄体期 (Luteal) ➡️ 秋：沉淀与内省
- **交互隐喻（流动感）**：首页使用“流体星球（Metaball）”等视觉元素，会随着用户的所在周期阶段与当下心情发生形状与色彩的动态变化。

## 3. 核心功能模块 (Key Features)

### 3.0 首次问卷 (Onboarding Questionnaire)
首次打开 App 时展示 5 道轻量问卷，优先让用户进入真实经期体验，不一上来询问周期长度。

- **题 1：你来月经时，最困扰你的是什么？**
  - 多选：小腹痛 / 腰酸、疲惫、情绪烦躁或低落、注意力下降、水肿/腹泻/胃不舒服、食欲变化、基本不困扰。
- **题 2：你的痛经通常到什么程度？**
  - 单选：几乎不痛、会痛但不影响生活、明显疼痛、影响学习/工作、严重到需要止痛药或无法活动、每次不太一样。
- **题 3：你有没有怀疑过，某些习惯会让你更不舒服？**
  - 多选：冰饮/生冷、咖啡/浓茶、吃辣、熬夜、压力大、久坐、运动太少、剧烈运动、还没注意过、影响不大。
- **题 4：这个周期，我们先一起观察一件事吧**
  - 单选：冷饮、咖啡、熬夜、运动、压力、清淡饮食、让心潮帮我发现规律、自定义填写。
- **题 5：为了提醒更准确，补充一点周期信息**
  - 可跳过：规律性、一次月经持续天数、痛感最明显时间。
- **生成页**
  - 标题：`已生成你的本周期观察目标`
  - 展示用户选择的观察目标，并说明不需要每天认真打卡，只需在相关习惯、痛感变化或不舒服时顺手记录。
  - 点击 `进入今日状态` 后保存 `onboardingCompleted`、问卷答案与 `observationGoal`。

### 3.1 首页：今日动态 (Today Page)
此页面作为用户每天打开后的核心状态页，参考 iPhone 健康的经期预测信息层级，优先呈现“今天在哪里、距离下次月经还有多久、可以记录什么”。

- **日期横条**：
  - 顶部展示今天附近 7 天日期。
  - 今天以圆形底色高亮，辅助用户快速定位当前日期。
  - 用户点击横条里的某一天后，首页立即预览该日期的周期阶段、距离经期天数、阶段背景与每日建议。
  - 右上角日历按钮可打开完整日期选择面板；面板按整月日历网格展示本月日期，支持切换上/下月。
  - 面板内选择日期后立即切换预览；`确认查看` 按钮已从预览模式移除。
  - 日历面板需要同时显示真实记录与预测：
    - 已记录 / 已发生的经期日：实心粉色圆 + 对勾。
    - 根据周期推算的本次经期持续日：粉色虚线圆 + 对勾。
    - 预测结束后的不确定缓冲日：灰色虚线圆。
  - 当前版本以 `月经开始日 + 默认经期长度` 推算经期持续范围；后续若增加“经期结束日”记录，可改为优先使用真实结束日。
- **月经预测主看板**：
  - 根据 `上次月经第一天 + 周期长度` 自动推算下次月经预计开始日。
  - 中央大字展示：
    - `距离经期还有 X 天`
    - 或 `预计今天进入经期`
    - 或 `当前处于经期`
  - 同时展示当前周期第几天。
- **阶段自动判断**：
  - 不再依赖首页底部“冬 / 春 / 夏 / 秋”手动切换。
  - 系统根据日期自动判断当前处于：
    - 月经期 / 冬
    - 卵泡期 / 春
    - 排卵期 / 夏
    - 黄体期 / 秋
  - 首页只显示当前阶段名称与阶段副标题。
- **女性群体共鸣细节**：
  - 首页阶段信息下方展示“我们与你同在 · WE ARE WITH YOU”与共渡人数文案，例如 `此刻有 12,504 位女性正在共同渡过`。
  - 不单独展示“同阶段共鸣”卡片；共鸣内容并入每日建议，在饮食 / 运动 / 工作建议下方作为一行小字出现。
  - 示例：`很多人经期前几天会安排轻松的活动`、`超过 56% 的人说：快来月经的时候食欲会变好`。
  - 当前 Demo 使用内置文案和体验样本数字，不伪装为真实平台统计；正式上线后可替换为匿名聚合数据。
- **本周期观察目标**：
  - 若用户完成首次问卷，首页展示本周期观察目标卡片，例如 `冷饮会不会影响我的痛感`。
  - 目标卡提示用户顺手记录相关习惯和不适变化，为周期结束后的规律回看做准备。
  - 观察目标卡内部采用类似 `每日建议 / 和潮聊天` 的双按钮结构，两个入口同等重要：
    - `记录相关状态`：进入与目标相关的记录模块。运动目标进入运动记录，疼痛/痛感目标进入症状记录，饮食/冷饮/咖啡目标进入饮食记录，压力/情绪目标进入情绪记录。
    - `查看洞察报告`：进入本周期/近期用户洞察报告。若当前记录不足，展示空状态：`再记录几次，心潮就能帮你看规律`。
- **虚化小岛背景**：
  - 保留原 `FloatingIsland` 的轮廓、构图与阶段视觉，不重新绘制新岛。
  - 小岛作为背景层居中偏上显示，使用轻微透明度与 blur 做雾化。
  - 四个阶段的小岛背景仍会根据当前阶段自动切换，形成“日期驱动的四季背景”。
- **三个核心记录入口**：
  - `记录月经期`：打开日历记录面板，由用户选择日期后记录或取消该日为月经开始日，并更新预测基准。
- **状态记录入口**：
  - 首页底部保留 `记录月经期` 与 `记录状态` 两个主按钮。
  - `记录月经期` 支持 toggle：未记录时点击会记录该日为月经开始日；已记录时再次点击会取消该日记录。
  - 若取消的是当前预测基准日，系统会尝试回退到最近一次仍保留的月经开始日；若没有历史记录，则清空预测基准。
  - 点击 `记录状态` 展开状态记录面板，包含：
    - 情绪
    - 症状
    - 饮食
    - 运动
    - 性行为
  - 每个状态分类内置与 Excel 字段表对齐的默认标签，采用顶部金刚位 + 横向滑动卡片展示，避免一次性占用过多版面。
  - 顶部金刚位固定展示五个模块，点击可直接切换；下方标签卡片支持左右滑动，滑动时顶部高亮与底部分页点同步更新。
  - 标签支持点击记录、再次点击取消；所选标签写入当天 journal。
  - `症状` 入口跳转到日记页继续记录疼痛、情绪与身体状态。
  - `性行为` 不再作为首页独立按钮，而是并入 `记录状态` 面板，在当天 journal 中记录一次性行为事件。
- **月经预测提醒**：
  - 如果用户开启提醒，并进入提前提醒窗口，首页展示温柔提醒文案：
    - 例如：`预计还有 3 天进入月经期，可以提前准备热饮和卫生用品。`
  - 若浏览器通知权限已开启，则同一预测周期内最多推送一次系统通知。
- **每日生活提示 (Daily Tips)**：
  - 保留饮食 / 运动 / 工作建议，但从首页主视觉降级为底部统一照顾区里的折叠区域。
  - 建议仍根据当前自动阶段、周期日、饮食偏好和运动偏好生成。
- **底部统一照顾区**：
  - 模块只提供两个同等重要的主按钮：`每日建议` 与 `和潮聊天`。
  - 点击 `每日建议` 展开饮食 / 运动 / 工作建议；点击 `和潮聊天` 直接进入潮的陪伴对话。
  - 三位角色不在首页底部展开，角色切换保留在聊天弹层内部。
  - 模块标题为 `接下来，你想做什么？`，包含：
    - `每日建议`：折叠展示饮食 / 运动 / 工作建议。
    - `和潮聊天`：进入 AI 陪伴对话。

### 3.2 历史与日记 (Journal Page)
集成了经期预测与情感记录的历史看板。
- **周期统计模块 (Cycle Tracker)**：
  - 计算展示：周期长度（平均）、经期长度（上次）、规律性（如“波动≤2天：稳定”）。
- **情绪日历网格**：
  - 日历上每一天用不同颜色的光球（StatusIcon）代表当天的情绪。点亮的光球反映该月情绪的“潮起潮落”。
- **单日记录 (Journal Entry)**：
  - **情感文字**：支持在自定义文本区域记录随感（自动保存）。
  - **身体疼痛记录**：通过底部弹窗形式，支持选择 0-5 级“疼痛程度”，并同时多选“疼痛位置”（如下腹、腰部、胸部等）。
- **疗愈回信 (Letter Panel)**：总结式的周报/月报系统，根据日记内容，通过AI（如Deepseek或备用方案）生成对用户的阶段性总结和回信慰藉。

### 3.3 个人偏好与设置 (About Page)
深度的定制化配置页面，确保AI与系统的推荐完全贴合用户的自身状况。
- **基础信息**：昵称填报。
- **排卵与经期数据**：
  - 上次月经第一天（驱动所有周期推算逻辑）。
  - 周期长度设置（默认 28 天，当前支持 21-45 天范围）。
- **月经预测与提醒设置**：
  - 展示预计下次月经日期与距离天数。
  - 支持开启 / 关闭预测提醒。
  - 支持选择提前 1 / 2 / 3 / 5 / 7 天提醒。
  - 支持请求浏览器通知权限；由于普通网页后台能力有限，浏览器通知仅作为 Demo 级能力，正式 App 级后台提醒后续需 PWA 或原生推送。
- **饮食偏好扩展面板**：
  - 标签与《4个周期P1标签》建议知识库对齐。
  - 硬性禁忌（乳糖不耐、海鲜过敏、坚果过敏、鸡蛋过敏、豆类过敏、麸质不耐）。
  - 饮食模式（无限制、素食、纯素、清真）。
  - 健康管理目标（控糖）。
  - 口味与敏感项（不辣 / 可能辣、油量、冷饮/生冷敏感、咖啡因敏感、辛辣敏感、高糖高油敏感等）。
  - 用户选择的饮食标签会作为大模型推荐的提示词上下文，用于生成更个性化的食物 / 饮品推荐。
- **运动偏好扩展面板**：
  - 标签与《4个周期P1标签》运动建议库对齐。
  - 运动频率（几乎不运动、每周1次、每周2-3次、每周4次以上、运动不稳定）。
  - 经期运动接受度（完全不想动、只能很轻、轻微痛时可以、和平时一样、让 App 判断）。
  - 运动场景（居家、户外、泳池、健身房）。
  - 重点避免（避免倒立、避免开胯、避免泡水、避免高强度、膝关节稳定）。

### 3.4 “潮”智能陪伴助手 (ChaoChat)
随时唤起的悬浮对话机器人，提供情感支撑与健康解惑。
- **灵活的陪伴人格 (Personas)**：支持随时在这三种人格间切换，满足不同时刻的心理需求：
  - **闺蜜 (Bestie)**：“我在的”
  - **母亲 (Mother)**：“孩子，怎么了”
  - **系统/潮汐 (Tide)**：“嗯，在听”
- **呼吸交互**：带有光晕流转的交互视觉（深呼吸动画），对话框使用磨砂玻璃质感。
- **知识库约束回答**：当用户询问月经、饮食、运动、疼痛缓解或身体反应机制时，后端会先检索本地知识库，再让角色基于检索内容回答；角色只负责语气差异，不负责凭空生成事实。
  - 推荐知识库：饮食、运动、生活疼痛建议。
  - 解释知识库：5 类高频身体反应及机制解释。
  - 没有检索到可靠内容时，AI 只能给低风险保守建议，并提醒继续观察或就医。
- **系统安全防线 (Safety & Crisis)**：探测用户输入的危险指令（如抑郁、自残关键词），可立即识别心理危机并附带（CRISIS_RESOURCE）专业救援指导和安全热线信息。

## 4. 技术栈及架构现状 (Tech Stack)
- **UI框架**: React 19 + TypeScript + Vite.
- **样式**: TailwindCSS (v4) + 包含高阶模糊与滤镜 (SVG Filters, backdrop-blur) 的原生 CSS。
- **动画**: Framer Motion (通过 `motion/react` 实现页面转场和弹窗动态)。
- **持久化状态管理**: 自定义 hook (`useInnertideStore`) 依托浏览器的 LocalStorage 同步记录配置与日记数据。
- **AI 智能驱动**: `@google/genai` (Google Gemini API接入，用于对话生成与长文本日记回信总结，代码已做好相关隔离和准备)。
- **路由**: 使用 Vite 原生的 Hash 监控 (Window location hash) 搭建了极简的多场景加载 (`/today`, `/journal`, `/about`)。

## 5. 当前实现补充记录 (Implementation Notes)

### 5.1 周期预测与提醒
- 新增 `getPeriodPrediction()`：
  - 输入：`lastPeriodStart`、`cycleLength`、`reminderDaysBefore`。
  - 输出：下次月经预计日期、提醒日期、距离天数、是否进入提醒窗口。
- 新增 `periodReminder` 本地状态：
  - `enabled`
  - `daysBefore`
  - `browserNotification`
  - `lastNotifiedPeriodKey`
- 所有设置写入 LocalStorage，不上传服务器。
- 首次问卷写入 `onboardingCompleted`、`onboardingAnswers` 与 `observationGoal`，均存储在 LocalStorage。

### 5.2 首页新信息架构
- 首页从“四阶段展示/预览型页面”调整为“周期预测主页面”。
- 阶段由 `computeCycleState()` 自动计算，不再提供底部四阶段手动预览栏。
- 原四阶段视觉仍保留在 `FloatingIsland`，但现在作为首页虚化背景。
- 首页保留“我们与你同在”的共渡人数文案；阶段共鸣句合并进 Daily Tips，不再独立占用一张卡片。
- 首页底部采用统一照顾区，将 Daily Tips 与 AI 入口合并为两个同级按钮，形成“记录 → 每日建议 / 和潮聊天”的顺序。
- 顶部日期横条支持“点击即预览”；右上角日历面板在预览模式下支持“选择日期即预览该日周期状态”。该行为只影响首页预览，不会直接改写周期数据。
- 右上角日历面板使用整月网格，读取 `journal[date].periodStarted` 与 `lastPeriodStart`，并结合默认经期长度展示经期范围：
  - 今天及以前的经期范围显示为实心粉色圆，表示已记录 / 已发生。
  - 今天之后的同一经期范围显示为粉色虚线圆，表示预测可能持续。
  - 预测结束后的 1 天显示为灰色虚线圆，表示不确定缓冲。
- 首页 `记录月经期` 进入同一日历面板的记录模式：选择日期后，主按钮根据当前日期状态显示 `记录为月经开始日` 或 `取消这天的月经记录`；未来日期不可记录。

### 5.3 周期复盘报告
- 报告主内容建议放在 `小岛日记 / Journal Page`，作为历史记录的一部分；首页不单独新增大卡片。
- 首页入口放在 `本周期观察目标` 卡片内，与 `记录相关状态` 并列为两个同级按钮，形式参考底部 `每日建议 / 和潮聊天`。
- 入口文案：
  - 主按钮：`查看洞察报告`
  - 空状态：`再记录几次，心潮就能帮你看规律`
  - 可用状态：`查看本周期复盘`
- 报告内容结构：
  - 标题：`本周期复盘：你离了解自己更近了一点`
  - 最高痛感、最难受时间。
  - 关键要素表格：疼痛记录、运动记录、情绪波动、疲惫记录；每项展示本周期数据、和上周期同一时期的对比、简短备注。
  - 本周期可能的规律：冰饮、运动、熬夜、情绪等与痛感/疲惫/烦躁的共现关系。
  - 下个周期建议：只做一个小实验，不要求用户一次改变所有习惯。
  - 数据提示：由于记录样本仍少，报告只作为观察线索，不做确定因果判断，建议继续记录 1-2 个周期。
- 当前 Demo 以“月经第 5 天”为展示场景，预置过去两周从黄体期到月经期的示例记录，用于让体验者直接看到周期复盘效果。

### 5.4 记录入口
- `记录月经期`：打开日历记录模式，用户选择日期后可记录或取消该日期的月经开始日。
- 再次选择已记录的月经开始日，会取消该日期的 `periodStarted`；若该日期也是 `lastPeriodStart`，则回退到最近一次仍保留的历史月经开始日。
- `记录状态`：展开状态面板，提供情绪、症状、饮食、运动、性行为五类入口。
- 每类入口采用“顶部金刚位 + 横向滑动标签卡片”的结构，默认标签与 Excel 动态打卡字段 D06-D09 对齐：
  - 情绪：平静、开心、有活力、情绪波动、焦虑、低落、易怒、疲惫。
  - 症状：一切正常、腹痛、腰酸、头痛、乳房胀痛、疲倦、水肿、失眠。
  - 饮食：正常、想吃甜、想吃辣、想喝热的、食欲差、胀气、外卖、喝水少。
  - 运动：没运动、散步、拉伸、瑜伽、力量训练、久坐、运动后舒服、适合休息。
  - 性行为：无、有、使用保护、未使用保护、事后不适、轻微疼痛、出血、不想记录细节。
- 症状卡片内增加两个次级区域：
  - 经期补充：经量少、经量中、经量多、担心侧漏，对应 D04/D05。
  - 已用缓解方式：热敷、止痛药、热饮、躺着、按摩、硬撑、拉伸、吐槽、都没用，对应 S04，并映射到生活疼痛建议库中的具体做法。
- 标签数据写入 `journal[date].statusTags`；性行为标签若记录实际发生状态，同时兼容写入 `sexualActivity.recordedAt`。

### 5.5 浏览器通知限制
- 本地 `localhost` 通常可测试通知权限。
- 云端若使用非 HTTPS，例如 `http://IP:端口`，部分浏览器会限制通知权限。
- 正式上线如需稳定后台提醒，需要 HTTPS + PWA Service Worker，或迁移到原生 App 推送。
